name: '[MaaS] Go build'

on:
  push:
    paths:
      - 'maas/**'
      - 'maas-integration-tests/**'
  pull_request:
    types: [ opened, synchronize, reopened ]
    paths:
      - 'maas/**'
      - 'maas-integration-tests/**'

jobs:
  build:
    uses: netcracker/qubership-core-infra/.github/workflows/generic-go-build.yaml@e8322e6ee168795edec1d103e289b3fe3ceb805d # v2.6.0
    with:
      sonar-project-key: ${{ vars.SONAR_PROJECT_KEY }}
      go-module-dir: maas/maas-service
      skip-docker: true
    secrets: inherit

  integration-tests:
    uses: netcracker/qubership-core-infra/.github/workflows/generic-maven-build.yaml@e8322e6ee168795edec1d103e289b3fe3ceb805d # v2.6.0
    with:
      maven-command: ${{ github.event_name == 'pull_request' && 'verify' || 'deploy' }}
      skip-docker: true
    secrets: inherit

  docker-build:
    needs: [build, integration-tests]
    uses: netcracker/qubership-core-infra/.github/workflows/docker-build.yaml@e8322e6ee168795edec1d103e289b3fe3ceb805d # v2.6.0
    with:
      dry-run: false
      config-filename: "maas---docker-dev-config.json"
